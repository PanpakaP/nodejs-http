doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    title アンケート
  body
    h1 どちらが好きですか？
    form(method="post" action=path)
      span 名前:
      input(type="text" name="name")
      input(type="radio" name="music" value=firstItem)
      span #{firstItem}
      input(type="radio" name="music" value=secondItem)
      span #{secondItem}
      button(type="submit") 投稿